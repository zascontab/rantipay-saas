root = "../.."  # Points to the monorepo root (kit/)
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/user ./cmd/user"  # Builds the user microservice
bin = "./tmp/user"                         # Runs the compiled binary
include_ext = ["go"]
exclude_dir = ["tmp", "vendor"]
delay = 1000